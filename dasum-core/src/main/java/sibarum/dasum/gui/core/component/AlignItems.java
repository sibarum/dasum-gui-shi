package sibarum.dasum.gui.core.component;

/**
 * How flex children are positioned along the cross axis.
 * {@code STRETCH} overrides a child's explicit cross-axis size to fill
 * the container's interior cross-axis dimension.
 */
public enum AlignItems { START, CENTER, END, STRETCH }
